# 🍽️ RestoBook — Система бронювання столиків

Веб-застосунок для онлайн-бронювання столиків у ресторанах з можливістю оформлення VIP-підписки, інтеграцією Telegram-бота та SMS-сповіщень.

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Laravel](https://img.shields.io/badge/Laravel-11-red)
![PHP](https://img.shields.io/badge/PHP-8.2-purple)

---

## Функціонал

- **Бронювання:** клієнти можуть обирати ресторан, столик, дату та час візиту.
- **VIP-клуб:** система платних підписок через **Monobank**.
- **Сповіщення:**
    - **Telegram Bot:** нагадування про бронювання та статус оплати.
    - **TurboSMS:** SMS-підтвердження замовлень.
- **Асинхронність:** відправка повідомлень через черги (Queues).
- **Адміністрування:** управління столиками та перегляд статистики.



##  Технічний стек

- **Backend:** PHP 8.2, Laravel 11
- **Frontend:** Blade Templates, Bootstrap 5, JavaScript
- **Database:** MySQL
- **Integrations:** Monobank API, Telegram Bot API, TurboSMS API
- **Tools:** Ngrok (для локального тестування вебхуків)



##  Встановлення та налаштування

### 1. Клонування репозиторію

```bash
git clone https://github.com/your-username/restobook.git
cd restobook
```

### 2. Встановлення залежностей

```bash
composer install
```

### 3. Налаштування оточення (.env)

Створіть файл конфігурації, скопіювавши приклад:

```bash
cp .env.example .env
```

Відкрийте файл `.env` та вкажіть параметри підключення до бази даних:

```env
DB_DATABASE=restaurant_booking
DB_USERNAME=root
DB_PASSWORD=secret
```

### 4. Генерація ключа та міграції

```bash
php artisan key:generate
php artisan migrate
```



##  Запуск проєкту

Для повноцінної роботи системи необхідно запустити **три процеси** у різних терміналах.

### 🔹 1. Локальний сервер (Terminal 1)

```bash
php artisan serve
```

Сайт буде доступний за адресою:  
http://localhost:8000



### 🔹 2. Обробник черг (Terminal 2)

**Важливо:** цей процес відповідає за відправку SMS та повідомлень у Telegram.  
Без нього сповіщення не надходитимуть.

```bash
php artisan queue:work
```



### 🔹 3. Тунель для Webhooks (Terminal 3)

```bash
ngrok http 8000
```

Після запуску скопіюйте HTTPS-адресу, наприклад:

```
https://random.ngrok-free.dev
```

та вставте її у файл `.env`:

```env
APP_URL=https://random.ngrok-free.dev
```



## Конфігурація (.env)

```env
APP_NAME=RestoBook
APP_ENV=local
APP_KEY=base64:generated_key
APP_DEBUG=true
APP_URL=https://random.ngrok-free.dev

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=restaurant_booking
DB_USERNAME=root
DB_PASSWORD=secret

TELEGRAM_BOT_TOKEN=your_telegram_token
TURBOSMS_API_KEY=your_turbosms_key
MONOBANK_TOKEN=test_monobank_token
```


##  Схема бази даних

Архітектура бази даних побудована на **реляційній моделі**.  
Центральним елементом є **Table (Столик)**, який зв’язує ресторан і клієнта.

### Зв’язки між сутностями

- **User ↔ Booking**  
  Один користувач може мати багато бронювань.

- **Restaurant ↔ Table**  
  Кожен столик належить конкретному ресторану.

- **Table ↔ Booking**  
  Бронювання прив’язується до конкретного столика.

- **Restaurant ↔ Booking**  
  Прямого зв’язку немає.  
  Приналежність визначається через столик (**транзитивний зв’язок**).



##  Висновок

Розроблений застосунок **RestoBook** дозволяє автоматизувати процес бронювання столиків у ресторані, забезпечує інтеграцію з платіжною системою Monobank та сервісами сповіщень, а також демонструє практичне використання сучасних можливостей фреймворку Laravel.
